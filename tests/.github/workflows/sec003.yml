# SEC-003: Self-Hosted Runner
name: Self-Hosted Runner Test
on: push
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - run: echo "on self-hosted"
  deploy:
    runs-on: [self-hosted, linux]
    steps:
      - run: echo "deploying"
