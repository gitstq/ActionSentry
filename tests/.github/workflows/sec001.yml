# SEC-001: Inherited Secrets
name: Inherited Secrets Test
on: push
jobs:
  call-reusable:
    runs-on: ubuntu-latest
    steps:
      - uses: my-org/reusable-workflow@main
        with:
          secrets: inherit
