# PIN-001: Unpinned Actions
name: Unpinned Actions Test
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - uses: actions/setup-python@master
      - uses: actions/cache@v3
      - uses: ./local-action
